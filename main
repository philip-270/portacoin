<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join Waitlist</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-image: url('/mnt/data/OpenPot.png'); /* Path to your uploaded image */
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        button {
            font-size: 24px;
            padding: 15px 30px;
            cursor: pointer;
            border: none;
            border-radius: 10px;
            background-color: #4CAF50;
            color: white;
            outline: none;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <button onclick="playSound()">Join Waitlist</button>

    <audio id="fartSound" src="/mnt/data/Fart sound effect.mp3"></audio> <!-- Path to your uploaded sound file -->

    <script>
        function playSound() {
            document.getElementById('fartSound').play();
        }
    </script>
</body>
</html>
